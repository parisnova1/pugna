# Base URL of the Pugna API server (see server/.env.example for its own
# config). Defaults to http://localhost:4000 if unset — only needed when
# pointing the web app at a deployed backend.
VITE_API_URL=http://localhost:4000
